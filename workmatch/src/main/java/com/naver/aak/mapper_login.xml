<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naver.aak.LoginDAO">

	<select id="loginCheck" parameterType="com.naver.aak.LoginDTO" resultType="int">
		select count(*) from member where mid=#{mid} and pwd=#{pwd}
	</select>
	
		<select id="midCheck" parameterType="com.naver.aak.LoginDTO" resultType="int">
		select count(*) from member where mid=#{mid}
	</select>
	
	<insert id="signUp" parameterType="com.naver.aak.LoginDTO">
		insert into member (
			mid,
			pwd
		) VALUES(
			#{mid},
			#{pwd}
		)
	</insert>
	
	<insert id="insertUserInfo" parameterType="com.naver.aak.LoginDTO">
		insert into user_info (
			name,
			mid,
			pwd,
			jumin_number1,
			jumin_number2,
			phone_number,
			email,
			occupation,
			location,
			address
		) VALUES(
			#{name},
			#{mid},
			#{pwd},
			#{jumin_number1},
			#{jumin_number2},
			#{phone_number},
			#{email},
			#{occupation},
			#{location},
			#{address}
		)
	</insert>
</mapper>